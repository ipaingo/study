name = "ba"

mi = 27
no = 0
for i in range(len(name)-1):
    print(ord(name[i]) - ord(name[i + 1]))
    if (ord(name[i]) - ord(name[i+1])) < mi:
        mi = ord(name[i]) - ord(name[i + 1])
        no = i
        print(mi, no)

name = name[0:no:] + name[no + 1::]

print(name)